#include <stdio.h>
#include "hashmap.h"

int main() {
    char buf[129];

    /* loop for a while */
    while (1) {
        /* read line */
        fscanf(stdin, "%128s", buf);

        /* put each token into the hashmap */
        while ()
    }

    return 0;
}
